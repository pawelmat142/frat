import { SelectorItem } from "global/interface/controls.interface";

interface Props {
    items: SelectorItem[]
    title: string
    multi?: boolean
}

const SelectorPopupContent: React.FC<Props> = ({ items, title, multi }) => {

    return null
}

export default SelectorPopupContent;